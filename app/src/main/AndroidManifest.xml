<?xml version="1.0" encoding="utf-8"?>
<!--suppress ALL -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Habiter"
        tools:targetApi="31">
        <activity
            android:name=".screen.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".screen.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".screen.SearchEmailActivity"
            android:exported="false" />


        <activity
            android:name=".screen.FormActivity"
            android:exported="false" />
        <activity
            android:name=".screen.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".screen.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".screen.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Habiter.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>